{
  "name": "tst-project",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "chmod +x build.sh && ./build.sh",
    "dev": "echo 'Development server not configured'",
    "start": "npx serve public -s --single"
  },
  "dependencies": {
    "serve": "^14.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}