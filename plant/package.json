{
  "name": "tst-project",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p public && cp -r ./* public/ 2>/dev/null || true && echo '{\"version\": 1}' > public/config.json",
    "dev": "echo 'No dev server' && exit 1",
    "start": "npx serve public -s"
  },
  "dependencies": {},
  "devDependencies": {}
}